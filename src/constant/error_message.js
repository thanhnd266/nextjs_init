
export function errorMessageRender (messageType)  {
    switch (messageType) {
        case "api_call":
            return "Có lỗi xảy ra vui lòng thử lại sau"
        default:
            return "Có lỗi xảy ra vui lòng thử lại sau"
    }
}

