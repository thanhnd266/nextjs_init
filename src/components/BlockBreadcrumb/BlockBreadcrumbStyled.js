import styled from "styled-components";

export const BlockBreadcrumbStyled = styled.div`
    padding: 2.4rem;
    background-color: ${(props) => props.$backgroundColor ? props.$backgroundColor : '#E8F6F2'};
`;